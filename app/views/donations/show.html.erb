<h1> Donation <%= @donation.id %> </h1>
<table>
  <th>Date</th> <th>Category/Crop</th> <th>Quantity</th> <th>Harvested from</th> <th>Donated to</th> <th>Comments</th>
  <tr>
    <td><%= @donation.date %></td> <td><%= @donation.category.name %></td> <td><%= @donation.quantity %></td>
    <td><%= @donation.donated_to %></td> <td><%= @donation.harvested_from %></td> <td><%= @donation.comments %></td>
    <td><%= link_to "Edit", edit_donation_path(@donation) %>
    <td><%= link_to "Delete", @donation, method: :delete %>
  </tr>
</table>
<%= link_to "Home", root_path %>
<%= link_to "View All", donations_path %>
